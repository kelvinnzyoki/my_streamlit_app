import Link from 'next/link'; import type { Program } from '@/types/program';
export default function ProgramCard({ program }: { program: Program }){ return <article className="card"><p className="eyebrow">{program.level} · {program.weeks} weeks</p><h3>{program.title}</h3><p className="muted">{program.description}</p><p className="gold">Focus: {program.focus}</p><Link className="btn btn-primary" href={`/programs/${program.id}`}>View Program</Link></article>}
