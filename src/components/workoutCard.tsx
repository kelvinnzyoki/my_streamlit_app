import Link from 'next/link'; import type { Workout } from '@/types/workout';
export default function WorkoutCard({ workout }: { workout: Workout }){ return <article className="card"><p className="eyebrow">{workout.category} · {workout.level}</p><h3>{workout.title}</h3><p className="muted">{workout.description}</p><p><span className="gold">{workout.duration} min</span> · {workout.calories} kcal</p><Link className="btn btn-primary" href={`/workouts/session?exercise=${workout.id}`}>Start Session</Link></article>}
