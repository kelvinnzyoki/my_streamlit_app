import DashboardShell from '@/components/DashboardShell';
export default function Admin(){const stats=[['Users','1,240'],['Active Plans','318'],['Feedback','27'],['Revenue','KES 182K']];return <DashboardShell><p className="eyebrow">Admin</p><h1>Admin Dashboard</h1><div className="grid grid-4">{stats.map(([k,v])=><div className="card" key={k}><p className="muted">{k}</p><div className="stat">{v}</div></div>)}</div><section className="section grid grid-2"><div className="card"><h3>Recent Activity</h3><p className="muted">Connect this panel to your admin API routes for users, subscriptions, audits, feedback and logs.</p></div><div className="card"><h3>Moderation</h3><p className="muted">Role-protect this route using backend checks before production.</p></div></section></DashboardShell>}
