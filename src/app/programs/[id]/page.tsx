import DashboardShell from '@/components/DashboardShell'; import { programs } from '@/lib/utils'; import Link from 'next/link';
export default function ProgramDetail({params}:{params:{id:string}}){const p=programs.find(x=>x.id===params.id)||programs[0];return <DashboardShell><p className="eyebrow">{p.level} · {p.weeks} weeks</p><h1>{p.title}</h1><p className="lead">{p.description}</p><div className="grid grid-2"><div className="card"><h3>Program Focus</h3><p className="gold">{p.focus}</p><p className="muted">Includes workouts, rest days, recovery reminders, and progress checks.</p></div><div className="card"><h3>Weekly Structure</h3><p className="muted">3–5 sessions per week depending on readiness and goal.</p><Link className="btn btn-primary" href="/workouts/session">Start First Session</Link></div></div></DashboardShell>}
