# FlowFit Next.js Frontend

Converted from the uploaded HTML/CSS/JS frontend into a TypeScript + React + Next.js App Router structure.

## Run
```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL=https://fit.cctamcc.site/api/v1` in `.env.local` if needed.
